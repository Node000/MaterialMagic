# 新手教程：文案与顺序（13 页，当前实现）

状态：**已按评审意见实施并通过 Play Mode 端到端回归**。教程仍是可重复进入的独立教学章节（`numericId = 100`）。

| 项目 | 位置 |
| --- | --- |
| 页面对象（Scene 直搭） | `Assets/Scenes/SampleScene_PC.unity` → `DebugBattleUI/TutorialRoot/Steps/<页面名>` |
| 页面文案（中/英） | `Assets/Resources/Data/Localization/zh-CN_Tutorial.json` / `en-US_Tutorial.json`（各 26 条 = 13 页 × 标题/正文） |
| 推进链、段落拆分、触发与门控 | `Assets/Scripts/TutorialManagerUI.cs` |
| 教学章节地图 | `Assets/Resources/Data/ChapterData.json`（`numericId = 100`）+ `HandSystemUI.BuildTutorialChapterMapGrid` |
| 高亮框 | 每页一个，由 `Steps/<页面>/Cutout` 的矩形决定 |

改文案只改上面两个 JSON 的 `text`（key 已按页面名对齐，无需动代码）。**注意**：在 Unity 外用工具改 JSON 后要让它强制导入（`AssetDatabase.ImportAsset`），否则 Resources 里还是旧文本。

## 阅读节奏（点击推进的粒度）

- **前 5 步**（`Map_Panel` / `Map_Movement` / `Map_StepLimit` / `Battle_Info` / `Battle_EnemyInfo`）：一次显示整段正文，点 1 次进入下一页。
- **其余步骤**：正文里每个 `>` 段落 = **一次点击推进**；同一页内推进段落时**高亮框保持不动，只替换文本框内容**。
  - 拆段的页面：`Battle_ArrowBase`(3)、`Battle_MagicBook`(3)、`Battle_PlayLimit`(3)、`Event_Options`(3) —— 全部段落都可点击推进。
  - `Battle_Refresh`(3)：前 2 段是阅读（点击推进），第 3 段是操作说明，读完后整屏拦截解除、等玩家完成换牌。
  - `Battle_Play`：只有 1 段，始终保持到点击「出手」。
- 拆段依据是文案表里的**空行**（`\n\n`）；想调整某页是否拆段，只改 `TutorialManagerUI.StepSplitsParagraphs` 里的名单。
- **每一段可以有自己的高亮框**：页面下 `Cutout` = 第 1 段，`Cutout2` = 第 2 段，`Cutout3` = 第 3 段（对应名字缺失/停用时自动回退 `Cutout`）。换段时只换高亮框目标，对话框文本与位置不变。
- 正文文本框字号：Body 已从 20 放大到 **30**（即 0.5 倍），并开启自动缩放（20–30）：段落短时用 30，较长段落自动缩到能装下（下限 20 = 原字号）。Title 保持 30 未改。

---

## 一、教学章节地图（已无商店格）

1 列 5 行，从下往上：

```text
Boss(1005)
休息(201)
事件(1002)
战斗(1001)
起点（空格）
```

商店不再出现在地图上，只在**战斗结算后自动进入**（关卡 401），商店教学页只在那一次出现。

## 二、实际顺序（已实施）

| 序 | 场景 | 触发点 | 页面（点击次数） | 类型 | 玩家动作 | 门控 |
| --- | --- | --- | --- | --- | --- | --- |
| 1-3 | 进入教学章节（首次选关） | `OnLevelSelectShown(0)` | `Map_Panel`(1) → `Map_Movement`(1) → `Map_StepLimit`(1) | 阅读 | 点击推进 | 锁定选关输入 + 整屏拦截 |
| 4-8 | 走到战斗格（固定抽 **上 上**） | `BeginTutorialBattle` + `TryApplyFixedTurnHand` 第 1 次 | `Battle_Info`(1) → `Battle_EnemyInfo`(1) → `Battle_ArrowBase`(3) → `Battle_MagicBook`(3) → `Battle_PlayLimit`(3) | 阅读 | 点击推进（每段一次） | 阅读页期间不可出牌/换牌/出手 |
| 9 | 同上 | 同上 | `Battle_Play`(—) | 行为 | 点手牌 → 点「出手」 | **换牌按钮禁用**；提示保持到点击「出手」 |
| 10 | 第 2 回合（固定抽 **右 右 右**） | `TryApplyFixedTurnHand` 第 2 次 | `Battle_Refresh`(2 次点击 + 操作) | 行为 | 放入任意张 → 点左下换牌按钮 | **出手按钮禁用**；**不限张数** |
| 11 | 第 2 回合换牌后 | `OnRefreshCompleted` | 无页面 | — | 自由出牌 → 出手（本回合结束敌人强制死亡） | 解锁自由操作 |
| 12 | 战斗胜利 | `OnRewardPanelShown` | `Reward_Claim`(1) | 阅读 | 点击推进 → 选 1 个奖励（或跳过） | 基础金币 +2 自动到手 |
| 13 | 战后自动商店（401） | `OnShopPanelShown` | `Shop_BuyHint`(1) | 阅读 | 点击推进 → 购买 | **只出现一次** |
| 14 | 走到事件格（1002，固定抽 **左 左**） | `OnEventOptionsShown` | `Event_Options`(3) | 阅读 | 点击推进（每段一次）→ 自行按组合选择/换牌 | 不强制换牌 |
| 15 | 走到休息格（201） | — | 无页面 | — | 正常休息流程 | 无教学页 |
| 16 | 走到 Boss 格（1005） | — | 无页面（抽 4 张） | — | 正常战斗 | 教学战斗限制已解除 |
| 17 | Boss 胜利 | `CompleteTutorial` | 隐藏全部页面 | — | — | 显示「恭喜你完成了新手教程！」 |

---

## 三、逐页文案（与 `zh-CN_Tutorial.json` 一致；`>` 分段即点击分段）

### 1. `Map_Panel` — 地图面板（1 次点击）
> 游戏的最终目标是击败Boss，你可以通过观察地图来规划路线。
>
> 地图上初始只会揭示Boss格位，你仅能解锁相邻格位的信息。

### 2. `Map_Movement` — 地图移动（1 次点击）
> 通过点击上、下、左、右方向的箭头，可以在地图上移动到相邻格子。
>
> 若你抵达的格子不为空，则会触发相应的内容。

### 3. `Map_StepLimit` — 步数限制（1 次点击）
> 当经过的关卡数达到上限时，所有格位都会变成Boss格位，强制进入Boss战。请在此之前做好准备。

### 4. `Battle_Info` — 玩家信息（1 次点击）
> 左上角是你的生命值、Buff和金币。

### 5. `Battle_EnemyInfo` — 敌人信息（1 次点击）
> 这里是敌人，敌人头顶显示的图标和数值代表了其本回合的意图。
>
> 若有多个敌人存在，可以通过点击敌人进行集火。（默认目标是最左边的敌人）

### 6. `Battle_ArrowBase` — 基础箭头（3 次点击，同一高亮框 = 手牌区）
> 6-1：这是你的手牌区，你在本回合抽取到的箭头会显示在这里。
>
> 6-2：这是你的出牌区，你准备打出/更换的箭头会显示在这里。
>
> 6-3：箭头分为四个方向：上、下、左、右。每个箭头在读取时会先触发自己的基础效果，随后进行道具匹配。

### 7. `Battle_MagicBook` — 玩家道具栏（3 次点击，同一高亮框 = 道具栏）
> 7-1：这是你的道具栏。出牌区的箭头组合满足道具配方时，会触发对应道具效果。
>
> 7-2：以同一个箭头为起点，若满足多个道具的要求，则会按从左到右的顺序依次触发道具效果。
>
> 7-3：你可以拖动道具来调整顺序。

### 8. `Battle_PlayLimit` — 出牌上限（3 次点击，同一高亮框 = 出牌上限显示）
> 8-1：出牌区与道具栏之间的数字是你的出牌限制。
>
> 8-2：更换/打出箭头时会占用出牌额度，当出牌额度用完后就无法出牌了。
>
> 8-3：点击手牌区的箭头将其移动到出牌区，点击出牌区的箭头可以取消出牌。

### 9. `Battle_Play` — 打出箭头（行为页，1 段）
> 点击手牌区的箭头将其移动到出牌区，然后点击右下角的“出手”确认出牌并结束回合。

限制：换牌按钮不可用（置灰 + 点击/快捷键都无效）；高亮框与提示一直保留到点击「出手」。

### 10. `Battle_Refresh` — 换牌（2 次点击 + 行为，同一高亮框 = 手牌区）
> 10-1（点击）：在抽到不想要的箭头时，可以点击“更换”按钮进行换牌。
>
> 10-2（点击）：你的回合开始时，换牌次数会重置为 1，通过某些方式可以临时或永久增加换牌次数。
>
> 10-3（操作）：点击手牌区的箭头将其移动到出牌区，然后点击左下角的按钮更换所有位于出牌区的箭头。

限制：读完前两段前整屏拦截（出手/换牌都不可点）；最后一段起出手按钮仍禁用、换牌按钮可用；**换牌不限张数**；换牌完成后解锁并收起提示。
“更换”按钮在界面上是左下角的**回收图标 + 次数角标**（无文字），文案按评审意见保持“更换”写法。

### 11. `Reward_Claim` — 结算奖励（1 次点击）
> 战斗胜利后会获得2个金币，然后你可以从3个额外奖励中选择1个获取。

### 12. `Shop_BuyHint` — 商店购买（1 次点击）
> 你可以在商店内购买新的道具和箭头，或者删除多余的箭头。
>
> 若要退出商店，请点击界面底部的红色宽箭头。

两句同属一段（单个换行分隔），仍为 1 次点击；英文表对应 `tutorial.shop.buy_hint.body` 同步追加一句。

### 13. `Event_Options` — 事件选项（3 次点击，同一高亮框 = 事件选项列）
> 13-1：每个事件会有多个选项，通过打出对应的箭头组合来选择，如果没有匹配的选项，则会自动触发无组合需求的选项。
>
> 13-2：点击手牌区的箭头将其移动到出牌区，然后点击“出手”按钮确认选项。
>
> 13-3：事件的抽牌基于你的牌组，若无法组成你想要的组合，同样可以进行换牌。

---

## 四、按段落的高亮框（本次新增，已逐段实测）

区域基准（画布/Steps 坐标，与项目默认 1920×1080 一致）：手牌区 `x -327..330 y -633..-469`；出牌区 `x -300..304 y -389..-295`；出手按钮 `x 540..815 y -556..-271`；换牌按钮 `x -742..-486 y -550..-278`；道具栏 `x -201..204 y -243..-147`；出牌上限显示 `x -86..90 y -310..-258`。高亮框 = 目标区域外拼再叠加遮罩自带的 18px 内边距。

| 页面 | 第 1 段（Cutout） | 第 2 段（Cutout2） | 第 3 段（Cutout3） |
| --- | --- | --- | --- |
| 6 `Battle_ArrowBase` | 手牌区 | **出牌区** | **手牌区 + 出牌区** |
| 7 `Battle_MagicBook` | 道具栏 | **道具栏 + 出牌区 + 手牌区** | 道具栏（拖动排序） |
| 8 `Battle_PlayLimit` | 出牌上限显示 | 出牌上限显示 | **手牌区 + 出牌区** |
| 9 `Battle_Play`（行为） | **手牌区 + 出牌区 + 出手按钮**（保持到点击「出手」） | — | — |
| 10 `Battle_Refresh`（行为） | 换牌按钮 | 换牌按钮 | **手牌区 + 出牌区 + 换牌按钮**（操作段） |
| 13 `Event_Options` | 事件选项列 | **手牌区 + 出牌区 + 出手按钮** | **手牌区 + 换牌按钮** |

未列出的页面（地图 3 页、`Battle_Info`、`Battle_EnemyInfo`、`Reward_Claim`、`Shop_BuyHint`）保持原来的单个高亮框（顶栏信息群 / 敌人区 / 结算三选一 / 商品区等）。

实现方式：`TutorialManagerUI.GetStepParagraphCutoutTarget()` 按段落序号取 `Cutout(N)`，缺省回退 `Cutout`；`ApplyParagraph()` 每次换段重设遮罩目标，所以框跟着段落走。想改某一页某一段的框位置，直接调该页对应 `CutoutN` 的矩形即可；想让它回到“整页单框”，把该页多余的 CutoutN 停用就行。

> ⚠ 分辨率提醒：高亮框是**固定矩形**，按项目默认 1920×1080（`ProjectSettings` 的 defaultScreenWidth/Height）编写；在最左侧/最右侧（两个按钮）这类远离中心的对象上，倾斜的 HUD 在非 1080p 的编辑器 Game view 下会投影出几十 px 的偏移。如果要做到分辨率无关，可以改成“每个区域序列化绑真实 UI 对象、运行时算洞”（已在 `plan/教程重做方案.md` §12.7 记过，需要时告诉我）。

> 想手动改框/改位置：见本文第九节。

## 五、其他教程相关设置

| 项 | 当前实现 |
| --- | --- |
| 教学页对象 | `DebugBattleUI/TutorialRoot/Steps` 下**只剩这 13 页**；旧 20 页版里从流程移除的 7 页（`Battle_Combo` / `Battle_Cancel` / `Reward_UndoHint` / `Shop_OrderHint` / `Shop_UndoHint` / `Event_Refresh` / `Rest_Options`）已从 `SampleScene_PC`（以及 `SampleScene_PE` / `PvOnlyScene` 里对应的 4 页 `Reward_EquipMagic` / `Reward_UndoHint` / `Shop_UndoHint` / `Event_Refresh`）一并删除；需要时可从 git 取回旧场景对象。 |
| 备注 | 未使用的 `CutoutN` 标记仍保留在部分页面（停用状态，如 `Battle_Play/Cutout2`），作为改段/换区域时的备用点。 |
| 教学初始金币 | **固定 10**（`TutorialManagerUI.TutorialStartGold`；覆盖「均衡」的初始值，不走进阶金币倍率、不影响普通新局） |
| 教学初始其他状态 | 走「均衡」配置：HP 50 / 抽 4 / 出牌 3 |
| 第 1 回合固定抽牌 | 火 火（上 上） |
| 第 2 回合固定抽牌 | 土 土 土（右 右 右） |
| 换牌脚本结果 | 按实际张数循环取「上 上 下」序列（放 3 张即上 上 下） |
| 事件固定抽牌 | 风 风（左 左） |
| 第 2 回合结束 | 教学敌人被强制击杀（不必打死） |
| 敌人兜底死亡 | 「教学中」2 层，每回合 -1，归零强制死亡 |
| 战斗基础金币 | 2（`BaseBattleGoldReward`） |
| 撤回教学 | 暂时不做（R / Backspace 撤回机制本身仍在） |

## 六、实现要点（便于后续维护）

- 段落拆分在 `TutorialManagerUI`：`StepSplitsParagraphs(step)` 声明要拆段的页面；`LocalizeSteps()` 缓存每页完整正文；`PrepareParagraphs()` 按空行拆分；`ApplyParagraph()` 只覆写 `Body` 文本，并按“是否还有下一段”重算点击推进与输入拦截 —— **段落推进不重新收集高亮框**，所以框会停在原处。
- 行为页的“最后一段”保持 `waitingForStepClick = false`，因此还是原来的行为门控（`Battle_Play` 等「出手」、`Battle_Refresh` 等换牌）。
- 拆段期间（`waitingForStepClick == true`）所有快捷键与两个动作按钮都被禁用，和整屏拦截一致。

## 七、验证记录（Play Mode，真实教学运行；未写入任何普通存档）

1. 开局：`chapter=100`、金币 **10**、HP 50；教学网格 5 格 = 空起点 → 1001 战斗 → 1002 事件 → 201 休息 → 1005 Boss，**无商店格**。
2. 阅读推进实测（点击顺序与段数）：`Battle_Info 1/1 → Battle_EnemyInfo 1/1 → Battle_ArrowBase 1/3 → 2/3 → 3/3 → Battle_MagicBook 1/3 → 2/3 → 3/3 → Battle_PlayLimit 1/3 → 2/3 → 3/3 → Battle_Play 1/1`，页内推进时**高亮框实例与矩形完全不变**（实测 `Cutout@360538 (1.5,-511)|(657,164)` 三段一致）。
3. `Battle_Play`：整屏拦截关闭、换牌按钮 disabled、出手 enabled；提示保持到点击「出手」。
4. `Battle_Refresh`：段 1/3、2/3 为阅读（整屏拦截、两按钮均禁用）→ 段 3/3 转为操作（拦截解除、换牌 enabled、出手仍 disabled）；空出牌区点击换牌被拒（`CanReplacePlayZone(0)=False`），放入 1 张即换牌成功（得 `Earth, Earth, Fire`），页面收起并解锁。
5. 出手 → 敌人强制死亡 → 结算（金币 10 → 12）→ 点击关闭 → 自动商店 401 → `Shop_BuyHint` → 点击关闭；再次触发 `OnShopPanelShown()` 不再出现。
6. 休息节点：无教学页、无整屏拦截，休息面板 2 个选项正常。
7. 主菜单回归：`StartScene` 的 `StartMenuUI` 初始化正常，点「教程」正常打开起始配置选择、返回正常关闭。
8. 编译 0 error；存档文件时间戳/内容未变化。

### 本轮（按段落高亮框 + 字号）补充验证

- 逐段高亮框实测（Play Mode，调 `ShowStep` 逐页逐段读取遮罩实际目标与投影矩形）：
  - `Battle_ArrowBase`：手牌区 → 出牌区 → 手牌+出牌（`x -327..330 y -633..-295`）
  - `Battle_MagicBook`：道具栏 → 道具栏+出牌+手牌（`x -327..330 y -633..-147`）→ 道具栏
  - `Battle_PlayLimit`：出牌上限 → 出牌上限 → 手牌+出牌
  - `Battle_Play`：手牌+出牌+出手按钮（`x -327..815 y -633..-271`），一直保持到点击「出手」
  - `Battle_Refresh`：换牌按钮 → 换牌按钮 → 手牌+出牌+换牌（`x -742..330 y -633..-278`，取消整屏拦截、只能换牌）
  - `Event_Options`：事件选项列 → 手牌+出牌+出手 → 手牌+换牌
- 字号实测：正文 Body 30（自动缩放 20–30）；例如 `Battle_EnemyInfo`（两段一起显示）自动缩到 ~22.9 以装下，其余单段落页面均为 30。

## 八、仍待你确认（非阻塞）

1. 撤回（R / Backspace）教学是否彻底不做，还是以后在别处补一句。
2. 「更换」按钮无文字（回收图标 + 角标）；若想让它显示“更换”需要 UI 侧加字。
3. 敌人名仍是「教学假人 / 高级假人」，设计文档写的是「教学人偶L / 教学人偶XXL」。
4. 仓库根目录还留着旧的 `Assets/StartScene-Backup.unity`（引用了已删除的旧 key），要不要清掉。
5. 前 5 步是否也要按段落拆（现在按你的要求是一次显示整段；要改只需在 `StepSplitsParagraphs` 里加名字）。

## 九、弹簧线参数（已对齐道具框）

高亮框的弹簧线与**道具框**（`MagicItemView.slotFrame`，即 `MagicSlot_PC` 根上的 `SpringLineHighlightUI`）逐项对齐，**只有颜色不同**（教程保持黄色 `(1, 0.84, 0.16)`）：

| 参数 | 道具框 | 教程高亮框 |
| --- | --- | --- |
| 条数 lineCount | 1 | **1** |
| 线宽 lineWidth | 5 | **5** |
| 外扩/内收 outset ↔ springInset | 2.7 | **2.7**（向内，同向意义） |
| 圈间距 lineSpacing | 0.2 | **0.2** |
| 采样 samplesPerLine | 56 | **56** |
| 圆角 roundedRectSharpness | 5 | **5** |
| 抖动 wobbleAmplitude | 8 | **8** |
| 波数 waveCount | 7 | **7** |
| 噪声 scribbleAmount | 3 | **3** |
| 切线抖动 tangentWobble | 0.18 | **0.18** |
| 相位偏移 linePhaseOffset | 0.045 | **0.045** |
| 种子 seed | 71 | **71** |
| 流动 flowSpeed | 0.7 | **0.7** |
| 脉冲 pulseAmount / pulseSpeed | 0.14 / 2.2 | **0.14 / 2.2** |
| 步进帧率 animationFramesPerSecond | 3 fps | **3 fps** |

- 改参数的位置：`Assets/Resources/Config/TutorialVisualConfig.asset` 的“弹簧线框”区（含新增的 `springSeed`）；代码默认值同步在 `TutorialVisualConfig.cs`。
- 几何：`loopRect = 洞矩形向内收 (springInset + i × springLineSpacing + 线宽/2)`，与 `SpringLineHighlightUI` 的 `Expand(rect, outset + i × spacing + 线宽/2)` 镜像一致（那一个是向外画、教程是贴洞内侧画）。
- 几何实测：对真实洞尺寸（出牌区/手牌区/出手按钮/道具栏/出牌上限/两个并集框）在 3fps 一个呼吸周期内抽样，弹簧墨迹（含半线宽）最多超出洞边界 **4.23px** —— 在 18px 的 padding 带内、不会碰到提示窗（洞到窗最近约 10px）。
- 注意：教程遮罩对象上的同名字段只是**序列化兵底**，运行时会由 `ApplyVisualConfig()` 用配置资产覆写；想单页例外就把该实例的 `useInstanceOverrides` 勾上。

## 十、手动调整位置在哪里

### 10.1 高亮框（洞）

路径：`Assets/Scenes/SampleScene_PC.unity` → `DebugBattleUI/TutorialRoot/Steps/<页面名>/`

| 子对象 | 用途 |
| --- | --- |
| `Cutout` | 第 1 段的高亮框 |
| `Cutout2` | 第 2 段（没有就回退 `Cutout`） |
| `Cutout3` | 第 3 段（没有就回退 `Cutout`） |

调它们的 RectTransform：`Pos X/Y` + `Width/Height`（锚点/轴心都是 0.5/0.5，单位是画布像素、相对该页面，页面中心为原点）。

关系的三层（重要）：

```text
目标区域（美术 UI 自己的矩形）
  → 洞（亮区边界） = 目标矩形 + (Cutout 位置偏移) + holePadding(18px)
    → 弹簧线 = 洞边界再向内收 springInset(2.7px)
```

所以：想让亮区回大就调大 `Cutout` 的宽高；想让框离目标更远就改 `holePadding`；想让线更靠内就改 `springInset`。填充/变暗不受影响。

### 10.2 文本框（提示窗）

- **窗口位置/大小**：页面对象自身 `Steps/<页面名>` 的 RectTransform —— `Pos X/Y` 是窗口中心（画布像素，屏幕中心为原点），`Width/Height` 是窗口尺寸（默认 620×190）。逐页位置就直接写在场景里（配置里的 `anchorTop/Bottom/Left/Right` 只是参考值，不参与运行）。
- **窗口内文字的位置/大小**：`Steps/<页面名>/TutorialStepWindow/Title` 与 `.../Body` 的 RectTransform。注意标题/正文在 `TutorialStepWindow` 预制体里是**拉伸锚点**，改 `Width/Height` 实际改的是 `sizeDelta`（真实尺寸 = 父宽/高 + sizeDelta）。
- **全局改（影响所有页）**：`Assets/Prefabs/UI/TutorialStepWindow.prefab` → `Title` / `Body` 的字号、对齐、内边距。
- **全局改（变暗/框样式/洞外扩/默认框尺寸）**：`Assets/Resources/Config/TutorialVisualConfig.asset`。

## 十一、当前页布局快照（2026-09-15）

本轮你在 Play Mode 里调了 13 页的 `Cutout*` 与文本框位置；退出 Play Mode 后已把运行期数值写回 `Assets/Scenes/SampleScene_PC.unity` 并保存（198 个 RectTransform 与运行期记录逐项比对，**0 差异**）。下表为保存后的实际值（画布像素，页面中心为原点）。

- 文本框的**可见位置 = 页根 `Steps/<页面名>` + 子对象 `TutorialStepWindow` 的偏移**（子对象是拉伸锚点，`sizeDelta` 为 0 时填满页根矩形，靠 `anchoredPosition` 整体位移）。
- 本轮改动的 8 处：
  - `Battle_ArrowBase/TutorialStepWindow` → (614, 145)；`Battle_MagicBook/TutorialStepWindow` → (557, -57)；`Battle_Play/TutorialStepWindow` → (-467, -44)；`Battle_PlayLimit/TutorialStepWindow` → (593, -65)
  - `Battle_ArrowBase/Cutout` → (0, -119)；`Battle_ArrowBase/Cutout3` → (0, -74)；`Battle_MagicBook/Cutout` → (0, -200)；`Shop_BuyHint/Cutout` → (8.5, 351.773) / 1037×554.453

| 页面 | 页根 anchoredPosition | 文本框(TutorialStepWindow) anchoredPosition | 各 Cutout（anchoredPosition / sizeDelta） |
| --- | --- | --- | --- |
| `Battle_ArrowBase` | (0, -345) | (614, 145) | Cutout: (0, -119) / 657×164<br>Cutout2: (2, 3) / 604×94<br>Cutout3: (0, -74) / 657×338 |
| `Battle_EnemyInfo` | (0, -105) | (0, 0) | Cutout: (-16, 338.5) / 520×467 |
| `Battle_Info` | (-500, 245) | (0, 0) | Cutout: (-69, 188) / 662×138 |
| `Battle_MagicBook` | (0, -25) | (557, -57) | Cutout: (0, -200) / 405×96<br>Cutout2: (1.5, -365) / 657×486 |
| `Battle_Play` | (0, -40) | (-467, -44) | Cutout: (193, -352.768) / 1249.58×383.483<br>Cutout2: (618, -380) / 217.455×237.463<br>Cutout3: (6.096, -436.22) / 791.191×164 |
| `Battle_PlayLimit` | (0, -150) | (593, -65) | Cutout: (2, -134) / 176×52<br>Cutout2: (2, -134) / 176×52<br>Cutout3: (1.5, -314) / 657×338 |
| `Battle_Refresh` | (0, -40) | (0, 0) | Cutout: (-614, -374) / 256×272<br>Cutout2: (-614, -374) / 256×272<br>Cutout3: (-163.173, -344.122) / 1157.65×372.131 |
| `Event_Options` | (0, -146) | (0, 0) | Cutout: (210, 277) / 388×268<br>Cutout2: (244, -306) / 1142×362<br>Cutout3: (-206, -309.5) / 1072×355 |
| `Map_Movement` | (0, -279) | (0, 0) | Cutout: (2, -190) / 478×162 |
| `Map_Panel` | (0, -265) | (0, 0) | Cutout: (0, 430) / 662×614 |
| `Map_StepLimit` | (645, 150) | (0, 0) | Cutout: (98, 302.5) / 228×103 |
| `Reward_Claim` | (0, -100) | (0, 0) | Cutout: (0, 265) / 646×162 |
| `Shop_BuyHint` | (0, -200) | (0, 0) | Cutout: (8.5, 351.773) / 1037×554.453 |

原始记录（临时目录，可随时再取）：`Library/Locus/tmp/tutorial_layout_playmode.json`（Play Mode 快照）、`tutorial_layout_editmode_after.json`（复现后校验）、`tutorial_layout_diff.json`（差异清单）。
