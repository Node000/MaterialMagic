	日期	bug属性	描述
1	6月18日	程序	具有电荷的箭头似乎可以通过反复进出出牌区，给对面加电弧效果【已修复：问题在于电荷附魔按入场触发，撤回重进会重复生效；已改为按 token 结算触发，复用现有读取/施法链路】
2	6月18日	程序	具有脆弱的箭头似乎无非被读取配方，也没有基础效果【已修复：问题在于 FragileArrowModifier 将匹配 token 数返回为 0，导致完全不参与读取；已改为保留原 token 数，分裂仍走现有读后处理链】
3	6月18日	美术	目前玩家动画的画质比静态素材低
4	6月18日	建议	电弧的施法触发好像是最后结算的，如果可以的话，感觉可以加个敌人的电弧条表示血量预期的扣减，包括火条等（类似杀戮尖塔的毒条）【已修复：按审阅意见不做新 UI 条；问题实质是电弧时机错误。现已改为玩家每打出一张牌时，立即按当前 Arc 层数对敌人结算一次伤害，不再等施法统一结算】？
5	6月18日	建议	电脑撤回的退格键在按键位置上有点奇怪（R也许会好一点【已修复：撤回输入已支持 R，同时保留 Backspace；教程与本地化文案已同步改为 R 键】
6	6月18日	建议	新增强化系统，休息的强化选项可以选择强化箭头或者强化法术，箭头升级后基础数值效果增加
7	6月18日	程序	四重上第一个打出，火酒只触发了两次【已修复：问题在于读取/施法只在每个 ArrowReadStep 的首 token 起点做一次匹配；已改为在该 step 覆盖的每个 token 起点做滑窗匹配，并按实际命中 token 区间触发演出与附魔】
8	6月18日	程序	卡牌附魔的漩涡好像没起效【已修复：问题与电荷相同，原先按入场触发导致时机错误；已改为按 token 结算触发，复用现有读取/施法链路】
9	6月18日	程序	假钻石移除的牌好像没进消耗堆，而且移除好像是永久的【已修复：问题在于事件删牌选择误走了 RemoveCardEverywhere 永久移除链；已改为走 ConsumeCardForBattle，使被移除的牌进入消耗堆】
10	6月18日	程序	苍蝇的换牌有bug，现在有回合结束时清除，但是实际上换牌生效应该在下回合，但是打出的这回合就被清除了【已修复：问题在于 next-draw 类 buff 在回合结束就被清空；已改为保留到下回合开始后再清理，并修正燃薪同类时序】
11	6月18日	建议	商店的箭头感觉需要和道具一样，带描述悬浮框【已修复：已复用现有 MaterialListPanelUI 的箭头说明 tooltip；商店箭头 hover 时会显示基础效果与附魔描述，不新增商店专用说明面板】
12	6月18日	美术	手机版后台换应用后背景会变成蓝色【已修复：问题在于相机 Viewport 留黑边后，黑边实际取的是 Camera 清屏色；游戏场景相机清屏色本身偏蓝，切后台恢复后会直接露出该颜色。现已统一在运行时强制相机使用黑色 Solid Color 作为黑边底色，并在恢复焦点/结束暂停时重新应用】
13	6月18日	美术	开始场景不是黑的，游戏场景是黑的，可能是Unity的问题，初始场景有个Unity的进入动画，导致背景被初始化为棕色【已修复：与 12 同根因，开始场景相机 Viewport 留黑边时使用了非黑色清屏底色；现已统一开始场景与游戏场景黑边底色为黑色，并在运行时强制保持】
    14	6月18日	美术	像蝇王、蓝眼招小弟的怪物，招出来后再对敌人鼠标悬停，鼠标离开后，敌人体积会变大【已修复：问题在于 JuicyMotion 只在初次 Awake 缓存 hover 回退基准，召唤/重排后敌人缩放变化却未同步，离开时回退到旧 scale；现已在敌人创建与刷新布局后同步更新基准 scale】

15	6月18日	程序	蝇王召唤的卵的亡语效果好像没有对主角造成伤害【已修复：问题在于 EnemyModel.Kill 先把生命置 0，再因 IsDead 提前返回，亡语链未触发；已改为按 dead/CurrentHealth 判定并正常触发 TriggerOnDie】
16	6月18日	建议	目前所有文字字号过小，手机端和电脑端阅读较为困难
17	6月18日	美术	手机端和电脑端开始卡组选择窗口内文字和内容过小，建议修改ui表现形式
18	6月18日	程序	焰似乎有bug，当前一回合给手牌附魔燃薪后，再次打出焰，下回合没有附魔燃薪【已修复：问题在于 next-draw 类 buff 在回合结束提前清空，且燃薪未正确消耗层数；已统一修正为下回合开始后清理并补上 ConsumeStack】
19	6月18日	美术	hover问题似乎在选择道具部分的提示框也出现了，提示框会变大【已修复：问题在于 tooltip 显示时总是重置到隐藏缩放，若上一次被父级 hover/选中动画放大后仍处于激活链中，会叠加出更大的提示框；现已在 MagicItemView 的提示框显示前清掉旧缩放状态，只在首次弹出时使用隐藏缩放动画】
20	6月18日	程序	敌人的意图随机，随机的意图不能和上一次的意图相同【已修复：底层随机意图选择缺少上一回合去重；已在 EnemyModel 通用随机意图选择中避开上一组意图（仅单一可选项除外）】
21	6月18日	程序	事件给的“下次战斗开始时获得4点护盾“效果在事件后选择的战斗开始后没有触发【已修复：问题在于战斗开始时先清护盾再触发/清理相关 buff，时序覆盖了事件护盾；已调整为 TriggerOnTurnStart -> ClearShield -> TriggerAfterTurnStart】
22	6月18日	程序	事件似乎不接受双重箭头使用，一个双重上不能触发对应的两个上选项【已修复：问题在于事件匹配按卡牌张数而非 token 语义；已改为使用 ArrowReadSequence.Tokens 进行匹配，支持双重/多重箭头】
23	6月18日	程序	目前boss彩虹似乎还没有打乱过玩家的出牌过【已修复：问题一是 PlayerState.ReturnPlayZoneCardsToDrawPile 实际错误地回到了弃牌堆；问题二是彩虹仅会随机手牌、不影响出牌区；现已修正回抽链路，并让彩虹随机追击后打乱玩家出牌区顺序】
24	6月18日	程序	事件 提供生命上限的同时，也增加了血量【已修复：问题在于事件直接调用 IncreaseMaxHealth，接口语义本身会同步回血；已新增 IncreaseMaxHealthOnly，并将事件改为只提升上限不改当前血量】
25	6月18日	美术	需要在角色血条那里显示生命上限？【已修复：血条文字改为显示 当前/上限；有护盾时显示 当前+护盾/上限】
26	6月18日	美术	在地图中行走时，获得保留附魔的箭头会留在画面中【已修复：问题在于离开节点回地图时只清了战斗状态，没有统一清掉已不属于 Hand/PlayZone 的卡牌视图；保留箭头在读后返还/切图后会留下残留 view。现已在回地图与结束节点时统一清理 orphaned card views】
27	6月19日	程序	手机端保存并退出，再继续游戏时没有恢复存档【已修复：问题在于运行存档只保存地图与玩家数据，未保存当前节点上下文；继续游戏回到节点时无法恢复节点内部状态。现已在 RunSaveSystem 中保存 currentNode，并在恢复时按当前节点重建对应状态】
28	6月23日	程序	商店界面，大退游戏，道具和箭头会直接刷新【已修复：问题在于商店每次打开都会重新生成商品，未持久化商品列表、已购状态、待选中的道具覆盖和撤销购买信息；现已保存并恢复 shop offers、selectedOffer、waitingForSelection、undo 状态，并在回到同一商店节点时复用】
29  6月24日 程序	开始界面，在点击“开始教程”并弹出初始配置后，点击“跳过教程”会导致“开始教程”的文本变为开始游戏，而实际期望的效果是取消“开始教程”的选择，回收弹窗后再跳过教程（删去教程按钮）【已修复：问题在于跳过教程未正确回退开始教程按钮状态；已在 StartMenuUI 中恢复按钮状态与流程文案】
30	6月24日	程序	移动端，在教程阶段，玩家进入战斗结算的道具奖励界面（三选一）后，点击道具后出现教程框引导玩家点击右侧道具栏进行替换，此处出现问题：在存在教程框的情况下，点击交互被认为是结束教程框，导致取消了道具奖励的选择，而再次点击道具后又会重新出现教程框（一个是取消选择的逻辑，一个是教程框应该只出现一次）【已修复：问题在于教程提示通过全局点击直接推进，和奖励道具替换的点击链共用同一帧输入，导致教程点击同时被底层槽位/面板逻辑消费；现已让教程优先独占这次点击，并将“点击右侧道具栏进行替换”的教程提示限制为只出现一次】
