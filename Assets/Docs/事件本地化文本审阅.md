# 事件本地化文本审阅（zh-CN）

- 用途：统一审阅 / 润色全部事件文本。**直接改「文本」列即可**，`event.*` 键名保持不动；改完发我，我回写 `Assets/Resources/Data/Localization/zh-CN_Event.json`。
- 本文件位置：`Assets/Docs/事件本地化文本审阅.md`（后续所有事件文本调整以本文件为准）。
- 数据来源：`Assets/Resources/Data/EventData.json`（结构、配方、效果）+ `Assets/Resources/Data/Localization/zh-CN_Event.json`（文本）。本文件按当前数据生成，数据改动后需重新生成。
- 效果列文案与游戏内 tooltip 一致（来自 `Assets/Scripts/UnifiedDetailContentBuilder.cs` 与 `zh-CN_UI.json`），可作为文本一致性参考。
- 配方字符映射：`1`=上(火)　`2`=左(风)　`3`=下(水)　`4`=右(土)；【乱序N张】= `randomRecipeLength=N` + `ignoreOrder`。
- 抽牌数：进入事件时抽多少张事件手牌；`-1` 表示跟随玩家当前抽牌数。
- 英文（`en-US_Event.json`）本轮未补，待翻译 key 清单见文末。

---

## 4 · 更多素材！

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_draw_training` |
| 抽牌数 | 0 |
| 标题 | 更多素材！ |
| 标题 key | `event.draw_training.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 你在试炼记录中整理上一段战斗的咒文经验，下一轮开始你能更快抽取素材。 | `event.draw_training.start.1` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 抽牌数量+1 | 空（不参与素材匹配，仅作退出/保底选项） | 每回合抽牌数+1 | `` | `event.draw_training.option.increase_draw` |

---

## 101 · 午夜便利店

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_midnight_convenience` |
| 抽牌数 | 3 |
| 标题 | 午夜便利店 |
| 标题 key | `event.midnight_convenience.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 凌晨两点，粉色霓虹把便利店门口照得像过曝的梦。 | `event.midnight_convenience.start.1` |
| 货架上只剩三样东西，扫码枪还在自言自语。 | `event.midnight_convenience.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 能量饮料 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 删除1张箭头（自选） | `energy_drink` | `event.midnight_convenience.option.energy_drink` |
| 薄荷糖 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 获得一次道具奖励 | `mint` | `event.midnight_convenience.option.mint` |
| 回家睡觉 | 空（不参与素材匹配，仅作退出/保底选项） | 恢复10点生命 | `leave` | `event.midnight_convenience.option.sleep` |

### 节点 `energy_drink`

| 正文 | key |
| --- | --- |
| 饮料把一段多余记忆冲掉了。选择一张素材，把它从牌组里删除。 | `event.midnight_convenience.energy_drink.1` |

### 节点 `mint`

| 正文 | key |
| --- | --- |
| 薄荷味在舌尖炸开，一个新道具广告弹了出来。 | `event.midnight_convenience.mint.1` |

### 节点 `leave`

| 正文 | key |
| --- | --- |
| 今晚熬不动了，你决定好好休息一晚。 | `event.midnight_convenience.sleep.1` |

---

## 102 · 扭蛋机

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_capsule_machine` |
| 抽牌数 | 4 |
| 标题 | 扭蛋机 |
| 标题 key | `event.capsule_machine.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 一台旧扭蛋机卡在巷子尽头，透明外壳里漂着彩色噪点。 | `event.capsule_machine.start.1` |
| 投币口在闪，旁边贴着“中奖率由心情决定”。 | `event.capsule_machine.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 投币 | 乱序 3 张（进事件时随机，不会与其他选项重复） | 获得3张相同的随机箭头 | `insert_coin` | `event.capsule_machine.option.insert_coin` |
| 给它一脚 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 失去4点生命 → 获得1张随机箭头 | `smash_open` | `event.capsule_machine.option.kick` |
| 给它一拳 | 空（不参与素材匹配，仅作退出/保底选项） | 失去2点生命 → 获得2金币 | `leave` | `event.capsule_machine.option.punch` |

### 节点 `insert_coin`

| 正文 | key |
| --- | --- |
| 扭蛋滚出来三次，里面是同一种素材的复制品。 | `event.capsule_machine.insert_coin.1` |

### 节点 `smash_open`

| 正文 | key |
| --- | --- |
| 塑料外壳碎了一地。你受了伤，但从机器肚子里掏出一张素材。 | `event.capsule_machine.smash_open.1` |

### 节点 `leave`

| 正文 | key |
| --- | --- |
| 你给了扭蛋机一拳，硬币槽咳出两枚硬币，你的手也隐隐作痛。 | `event.capsule_machine.punch.1` |

---

## 103 · 匿名论坛

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_anonymous_forum` |
| 抽牌数 | 3 |
| 标题 | 匿名论坛 |
| 标题 key | `event.anonymous_forum.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 你刷到一个匿名论坛，头像全是默认图标，帖子却像活物一样刷新。 | `event.anonymous_forum.start.1` |
| 热帖标题闪烁着：“来都来了，说点什么吧。” | `event.anonymous_forum.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 抬杠 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 删除1张箭头（自选） → 恢复10点生命 | `argue` | `event.anonymous_forum.option.argue` |
| 发帖 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 失去3点生命 → 获得一次道具强化 | `post` | `event.anonymous_forum.option.post` |
| 跟贴 | 空（不参与素材匹配，仅作退出/保底选项） | 恢复4点生命 | `leave` | `event.anonymous_forum.option.reply` |

### 节点 `argue`

| 正文 | key |
| --- | --- |
| 你赢了一场没有观众的争论，顺手删掉一张不顺眼的素材，并恢复生命值。 | `event.anonymous_forum.argue.1` |

### 节点 `post`

| 正文 | key |
| --- | --- |
| 帖子发出后，评论区反噬了你的精神值，但也给了你一次道具强化。 | `event.anonymous_forum.post.1` |

### 节点 `leave`

| 正文 | key |
| --- | --- |
| 你认真跟了一句，陌生人的认同感让你放松下来，立即恢复4点生命。 | `event.anonymous_forum.reply.1` |

---

## 104 · 咖啡还是可乐？

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_coffee_or_cola` |
| 抽牌数 | 2 |
| 标题 | 咖啡还是可乐？ |
| 标题 key | `event.coffee_or_cola.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 自动贩卖机的屏幕裂成两半，一边是咖啡，一边是可乐。 | `event.coffee_or_cola.start.1` |
| 它问你：“清醒，还是更清醒？” | `event.coffee_or_cola.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 我全都要 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 恢复10点生命 → 生命上限+5 | `all_in` | `event.coffee_or_cola.option.all` |
| 咖啡 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 删除1张箭头（自选） | `coffee` | `event.coffee_or_cola.option.coffee` |
| 可乐 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 生命上限+5 | `cola` | `event.coffee_or_cola.option.cola` |
| 白开水更健康 | 空（不参与素材匹配，仅作退出/保底选项） | 恢复5点生命 | `leave` | `event.coffee_or_cola.option.water` |

### 节点 `all_in`

| 正文 | key |
| --- | --- |
| 你把两罐混在一起喝了。味道很危险，但生命值和上限都提高了。 | `event.coffee_or_cola.all.1` |

### 节点 `coffee`

| 正文 | key |
| --- | --- |
| 咖啡苦得像待办事项。你清醒过来，决定删掉一张不该继续留着的素材。 | `event.coffee_or_cola.coffee_remove.1` |

### 节点 `cola`

| 正文 | key |
| --- | --- |
| 气泡在血管里跑圈。你的生命上限提高了。 | `event.coffee_or_cola.cola.1` |

### 节点 `leave`

| 正文 | key |
| --- | --- |
| 你接了一杯白开水，身体没有抗议，反而悄悄恢复了一点精神。 | `event.coffee_or_cola.water.1` |

---

## 105 · 限时特惠！

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_limited_sale` |
| 抽牌数 | 4 |
| 标题 | 限时特惠！ |
| 标题 key | `event.limited_sale.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 商场大屏把你的脸识别成“潜在冲动消费者”。 | `event.limited_sale.start.1` |
| 弹窗一层叠一层：只有现在，错过再等永远。 | `event.limited_sale.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 好看的衣服 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 生命上限+5 | `clothes` | `event.limited_sale.option.clothes` |
| 好看的武器 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 获得一次道具强化 | `weapon` | `event.limited_sale.option.weapon` |
| 不买立省百分百 | 空（不参与素材匹配，仅作退出/保底选项） | 获得2金币 | `no_buy` | `event.limited_sale.option.no_buy` |

### 节点 `clothes`

| 正文 | key |
| --- | --- |
| 新衣服像护甲一样贴合。你的生命上限提高了。 | `event.limited_sale.clothes.1` |

### 节点 `weapon`

| 正文 | key |
| --- | --- |
| 武器只是贴纸，但贴纸真的改变了你的道具。 | `event.limited_sale.weapon.1` |

### 节点 `no_buy`

| 正文 | key |
| --- | --- |
| 你关掉所有弹窗，发现最划算的购买方案就是不买。系统不情愿地吐出2金币当作“理性消费奖励”。 | `event.limited_sale.no_buy_save.1` |

---

## 106 · 推广链接

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_promo_link` |
| 抽牌数 | 4 |
| 标题 | 推广链接 |
| 标题 key | `event.promo_link.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 一个弹窗追着你跑：“分享给朋友，立刻获得奖励！” | `event.promo_link.start.1` |
| 下面的小字写着：本活动可能造成轻微社死或生命流失。 | `event.promo_link.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 分享链接 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 失去3点生命（每次重复+1） → 获得2金币 | `repeat` | `event.promo_link.option.share` |
| 适可而止 | 空（不参与素材匹配，仅作退出/保底选项） | 无直接效果 | `leave` | `event.promo_link.option.stop` |

### 节点 `repeat`

| 正文 | key |
| --- | --- |
| 链接已经发出，金币到账。弹窗变得更兴奋了：要不要再分享一次？ | `event.promo_link.repeat.1` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 继续分享链接 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 失去3点生命（每次重复+1） → 获得2金币 | `repeat` | `event.promo_link.option.share_again` |
| 适可而止 | 空（不参与素材匹配，仅作退出/保底选项） | 无直接效果 | `leave` | `event.promo_link.option.stop` |

### 节点 `leave`

| 正文 | key |
| --- | --- |
| 你关掉弹窗。它在关闭动画里还试图说服你。 | `event.promo_link.leave.1` |

---

## 107 · 流量增长

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_traffic_growth` |
| 抽牌数 | 0 |
| 标题 | 流量增长 |
| 标题 key | `event.traffic_growth.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 把闯关视频发布到平台上，几分钟后播放量像霓虹浪潮一样冲上来。弹幕和点赞把屏幕挤满，你忽然觉得下一段旅程可以更快一点。 | `event.traffic_growth.start.video.1` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 好耶！！！ | 空（不参与素材匹配，仅作退出/保底选项） | 每回合抽牌数+1 | `growth` | `event.traffic_growth.option.cheer` |

### 节点 `growth`

| 正文 | key |
| --- | --- |
| 平台的流量推着你向前。之后每回合抽牌数+1。 | `event.traffic_growth.growth.video.1` |

---

## 108 · 箭头回收站

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_arrow_recycle_station` |
| 抽牌数 | 2 |
| 标题 | 箭头回收站 |
| 标题 key | `event.arrow_recycle_station.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 一台旧终端嵌在墙里，屏幕上跳着“回收多余箭头”。 | `event.arrow_recycle_station.start.1` |
| 它会读取你打出的箭头序列，并启动对应强度的回收程序。 | `event.arrow_recycle_station.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 轻量回收 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 删除1张箭头（自选） | `single_recycle` | `event.arrow_recycle_station.option.single` |
| 深度回收 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 删除2张箭头（自选） | `double_recycle` | `event.arrow_recycle_station.option.double` |
| 离开 | 空（不参与素材匹配，仅作退出/保底选项） | 无直接效果 | `leave` | `event.common.option.leave` |

### 节点 `single_recycle`

| 正文 | key |
| --- | --- |
| 终端把一个箭头压缩成发烫的数据碎片。你的牌组变得更轻了一点。 | `event.arrow_recycle_station.single.1` |

### 节点 `double_recycle`

| 正文 | key |
| --- | --- |
| 终端连续轧下两次，两个箭头被压成细小的光屑。你的牌组明显轻了一截。 | `event.arrow_recycle_station.double.1` |

### 节点 `leave`

| 正文 | key |
| --- | --- |
| 你关掉回收程序，终端屏幕慢慢暗了下去。 | `event.arrow_recycle_station.leave.1` |

---

## 109 · 箭头娃娃机

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_arrow_claw_machine` |
| 抽牌数 | 2 |
| 标题 | 箭头娃娃机 |
| 标题 key | `event.arrow_claw_machine.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 一台娃娃机亮着霓虹灯，玻璃柜里堆满了上下左右的箭头。 | `event.arrow_claw_machine.start.1` |
| 机械爪悬在半空，只会响应你打出的同方向箭头。 | `event.arrow_claw_machine.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 抓上箭头 | 上箭头（按顺序） | 获得1张上箭头 | `catch_up` | `event.arrow_claw_machine.option.up` |
| 抓下箭头 | 下箭头（按顺序） | 获得1张下箭头 | `catch_down` | `event.arrow_claw_machine.option.down` |
| 抓左箭头 | 左箭头（按顺序） | 获得1张左箭头 | `catch_left` | `event.arrow_claw_machine.option.left` |
| 抓右箭头 | 右箭头（按顺序） | 获得1张右箭头 | `catch_right` | `event.arrow_claw_machine.option.right` |
| 离开 | 空（不参与素材匹配，仅作退出/保底选项） | 无直接效果 | `leave` | `event.common.option.leave` |

### 节点 `catch_up`

| 正文 | key |
| --- | --- |
| 机械爪晃晃悠悠地落下，夹起一个发光的上箭头。新的箭头落入你的牌组。 | `event.arrow_claw_machine.up.1` |

### 节点 `catch_down`

| 正文 | key |
| --- | --- |
| 机械爪晃晃悠悠地落下，夹起一个发光的下箭头。新的箭头落入你的牌组。 | `event.arrow_claw_machine.down.1` |

### 节点 `catch_left`

| 正文 | key |
| --- | --- |
| 机械爪晃晃悠悠地落下，夹起一个发光的左箭头。新的箭头落入你的牌组。 | `event.arrow_claw_machine.left.1` |

### 节点 `catch_right`

| 正文 | key |
| --- | --- |
| 机械爪晃晃悠悠地落下，夹起一个发光的右箭头。新的箭头落入你的牌组。 | `event.arrow_claw_machine.right.1` |

### 节点 `leave`

| 正文 | key |
| --- | --- |
| 你松开操纵杆，娃娃机里的箭头继续闪着霓虹光。 | `event.arrow_claw_machine.leave.1` |

---

## 110 · 贴膜摊

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_screen_protector_stall` |
| 抽牌数 | 3 |
| 标题 | 贴膜摊 |
| 标题 key | `event.screen_protector_stall.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 路边小摊挂着一排会发光的保护膜，摊主说每一张都能贴在箭头上。 | `event.screen_protector_stall.start.1` |
| 他熟练地甩出刮板，气泡在霓虹灯下像小型道具事故。 | `event.screen_protector_stall.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 免费贴膜 | 空（不参与素材匹配，仅作退出/保底选项） | 选择1张箭头获得【保留】 | `free_film` | `event.screen_protector_stall.option.free` |
| 劣质膜 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 选择1张箭头获得【脆弱】 | `cheap_film` | `event.screen_protector_stall.option.cheap` |
| 钢化膜 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 选择1张箭头获得【永恒】 | `tempered_film` | `event.screen_protector_stall.option.tempered` |

### 节点 `free_film`

| 正文 | key |
| --- | --- |
| 免费膜意外地很服帖。选择一张箭头，让它在手里多待一会儿。 | `event.screen_protector_stall.free.1` |

### 节点 `cheap_film`

| 正文 | key |
| --- | --- |
| 膜一贴上就裂出漂亮纹路。选择一张箭头，让它变得脆弱。 | `event.screen_protector_stall.cheap.1` |

### 节点 `tempered_film`

| 正文 | key |
| --- | --- |
| 钢化膜发出清脆的咔哒声。选择一张箭头，让它下回合也会回来。 | `event.screen_protector_stall.tempered.1` |

---

## 111 · 打印机事故

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_printer_accident` |
| 抽牌数 | 2 |
| 标题 | 打印机事故 |
| 标题 key | `event.printer_accident.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 一台自助打印机正在吐出方向箭头，纸张边缘冒着热气。 | `event.printer_accident.start.1` |
| 屏幕上写着“打印成功”，但你很确定它什么都没对齐。 | `event.printer_accident.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 看不出来 | 空（不参与素材匹配，仅作退出/保底选项） | 获得2金币 | `unclear` | `event.printer_accident.option.unclear` |
| 大小不对 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 选择1张箭头获得【双重箭头】 | `wrong_size` | `event.printer_accident.option.wrong_size` |
| 错位了 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 选择1张箭头获得【重复】 | `misaligned` | `event.printer_accident.option.misaligned` |

### 节点 `unclear`

| 正文 | key |
| --- | --- |
| 你决定假装没看懂。打印机退回两枚硬币作为精神损失费。 | `event.printer_accident.unclear.1` |

### 节点 `wrong_size`

| 正文 | key |
| --- | --- |
| 打印比例被拉大了。选择一张箭头，把它变成更占配方格的多重箭头。 | `event.printer_accident.wrong_size.1` |

### 节点 `misaligned`

| 正文 | key |
| --- | --- |
| 同一个箭头被印了两遍。选择一张箭头，让它读取时变得更重。 | `event.printer_accident.misaligned.1` |

---

## 112 · 网站突发问题

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_website_emergency` |
| 抽牌数 | 2 |
| 标题 | 网站突发问题 |
| 标题 key | `event.website_emergency.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 你打开一个本该显示地图的网站，页面却开始疯狂抖动。 | `event.website_emergency.start.1` |
| 错误提示一层叠一层，像一群急着报名的怪物。 | `event.website_emergency.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 刷新就好 | 空（不参与素材匹配，仅作退出/保底选项） | 恢复4点生命 | `refresh` | `event.website_emergency.option.refresh` |
| 网页未响应 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 选择1张箭头获得【保留】 | `reversed_direction` | `event.website_emergency.option.reversed_direction` |
| 好多弹窗 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 选择1张箭头获得【增殖】 | `popups` | `event.website_emergency.option.popups` |

### 节点 `refresh`

| 正文 | key |
| --- | --- |
| 你按下刷新键，页面短暂恢复正常，连带着你的精神也恢复了一点。 | `event.website_emergency.refresh.1` |

### 节点 `reversed_direction`

| 正文 | key |
| --- | --- |
| 网页卡死在加载圈里。选择一张箭头，让它在回合结束时继续留在手牌。 | `event.website_emergency.reversed_direction.1` |

### 节点 `popups`

| 正文 | key |
| --- | --- |
| 弹窗越关越多。选择一张箭头，让它学会在打出后复制自己。 | `event.website_emergency.popups.1` |

---

## 113 · 全向夜航牌

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_neon_wayfinding` |
| 抽牌数 | 2 |
| 标题 | 全向夜航牌 |
| 标题 key | `event.neon_wayfinding.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 凌晨三点，废弃商场的导视牌还亮着。粉紫色箭头在空荡走廊里来回切换：出口、厕所、电玩厅、地下停车场……最后所有箭头同时指向你。 | `event.neon_wayfinding.start.1` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 跟着所有箭头走 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 获得1张万能箭头，带【万向箭头】 | `follow_all_arrows` | `event.neon_wayfinding.option.follow_all_arrows` |
| 只拍一张照 | 空（不参与素材匹配，仅作退出/保底选项） | 获得2金币 | `take_photo` | `event.neon_wayfinding.option.take_photo` |

### 节点 `follow_all_arrows`

| 正文 | key |
| --- | --- |
| 导视牌同时熄灭，又在你手里重新亮起。你获得一张万向符号。 | `event.neon_wayfinding.follow_all_arrows.1` |

### 节点 `take_photo`

| 正文 | key |
| --- | --- |
| 照片糊得只剩紫色光斑，但有人愿意为这张怪照片付钱。 | `event.neon_wayfinding.take_photo.1` |

---

## 114 · 疑似木马？

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_suspected_trojan` |
| 抽牌数 | 3 |
| 标题 | 疑似木马？ |
| 标题 key | `event.suspected_trojan.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 一个不断变化着的箭头在你的手机上闪动着。似乎是因为下载了什么木马程序？不过因为型号老旧，它也没能找到攻击的途径，只能徒劳地闪烁着。 | `event.suspected_trojan.start.1` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 这样就好 | 空（不参与素材匹配，仅作退出/保底选项） | 获得1张万能箭头，带【随机箭头】 | `keep_it` | `event.suspected_trojan.option.keep_it` |
| 重装系统 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 花光所有金币 → 将牌库中的基础箭头随机重置（保留附魔） | `reinstall_system` | `event.suspected_trojan.option.reinstall_system` |

### 节点 `keep_it`

| 正文 | key |
| --- | --- |
| 你决定不管它。那枚闪动的箭头跳进牌组，继续变化着方向。 | `event.suspected_trojan.keep_it.1` |

### 节点 `reinstall_system`

| 正文 | key |
| --- | --- |
| 进度条跑到百分之一百，你的钱包也被清空。所有箭头都像刚刷完机一样换了方向。 | `event.suspected_trojan.reinstall_system.1` |

---

## 115 · 字体丢失

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_missing_fonts` |
| 抽牌数 | 3 |
| 标题 | 字体丢失 |
| 标题 key | `event.missing_fonts.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 夜班打印店的电脑弹出一串乱码，所有字体都丢了，只剩几个符号还顽固地显示着。你一通操作，弹出了一个“是否关闭兼容模式”的弹窗。 | `event.missing_fonts.start.1` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 关闭兼容模式 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 获得1张随机语法箭头 | `disable_compatibility` | `event.missing_fonts.option.disable_compatibility` |
| 保持兼容模式 | 空（不参与素材匹配，仅作退出/保底选项） | 获得2金币 | `keep_compatibility` | `event.missing_fonts.option.keep_compatibility` |

### 节点 `disable_compatibility`

| 正文 | key |
| --- | --- |
| 屏幕黑了一下，随后吐出一个还没来得及变成文字的符号。你获得一张随机句法符号。 | `event.missing_fonts.disable_compatibility.1` |

### 节点 `keep_compatibility`

| 正文 | key |
| --- | --- |
| 你保持原样。打印店老板对你的克制表示感谢，塞给你几枚硬币。 | `event.missing_fonts.keep_compatibility.1` |

---

## 116 · 闲置交易平台

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_idle_trade_platform` |
| 抽牌数 | 3 |
| 标题 | 闲置交易平台 |
| 标题 key | `event.idle_trade_platform.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 二手平台的界面泛着旧主机的荧光，卖家头像全是默认的灰色小人。 | `event.idle_trade_platform.start.1` |
| 三个挂着“闲置”标签的箭头排成一列，备注写着：一口价，砍价免谈。 | `event.idle_trade_platform.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 砍到最后 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 获得一次道具奖励 | `cut_to_last` | `event.idle_trade_platform.option.cut_to_last` |
| 一口价拍下 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 失去3金币 → 获得一次道具奖励 | `buyout` | `event.idle_trade_platform.option.buyout` |
| 只是来签到 | 空（不参与素材匹配，仅作退出/保底选项） | 获得2金币 | `check_in` | `event.idle_trade_platform.option.check_in` |

### 节点 `cut_to_last`

| 正文 | key |
| --- | --- |
| 你把价格砍到卖家放弃回复。屏幕一暗，一件商品直接落进你的道具栏。 | `event.idle_trade_platform.cut_to_last.1` |

### 节点 `buyout`

| 正文 | key |
| --- | --- |
| 你按下“立即购买”，金币少了3枚。卖家秒发货，一句谢谢都没有。 | `event.idle_trade_platform.buyout.1` |

### 节点 `check_in`

| 正文 | key |
| --- | --- |
| 你只点了签到。平台照例给了2金币，然后开始给你推广告。 | `event.idle_trade_platform.check_in.1` |

---

## 117 · 数据安全

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_data_backup` |
| 抽牌数 | 2 |
| 标题 | 数据安全 |
| 标题 key | `event.data_backup.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 维护前的备份提醒弹了出来，磁盘进度条卡在0.3%已经很久。 | `event.data_backup.start.1` |
| 提示写得很客气：全量备份耗时较长，请注意剩余空间。 | `event.data_backup.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 全量备份 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 获得3张随机方向箭头，带【临时】 | `full_backup` | `event.data_backup.option.full_backup` |
| 重点备份 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 获得1张随机方向箭头，带【临时】 | `key_backup` | `event.data_backup.option.key_backup` |
| 只复制快捷方式？ | 空（不参与素材匹配，仅作退出/保底选项） | 牌组中随机50%的箭头获得【临时】 | `shortcut_only` | `event.data_backup.option.shortcut_only` |

### 节点 `full_backup`

| 正文 | key |
| --- | --- |
| 进度条走到100%。三枚半透明的箭头被完整复制出来——它们不占打出次数，离开手牌就会消失。 | `event.data_backup.full_backup.1` |

### 节点 `key_backup`

| 正文 | key |
| --- | --- |
| 你只备份了最关键的那一份。一枚【临时】箭头落进牌组，轻得几乎没有重量。 | `event.data_backup.key_backup.1` |

### 节点 `shortcut_only`

| 正文 | key |
| --- | --- |
| 备份变成了快捷方式：牌组里一半的箭头被随手贴上【临时】标签，指向原件的影子。 | `event.data_backup.shortcut_only.1` |

---

## 118 · 今天是什么日子？

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_what_day_is_it` |
| 抽牌数 | 3 |
| 标题 | 今天是什么日子？ |
| 标题 key | `event.what_day_is_it.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 日历插件崩了，日期位置只剩“节假日 / 休息日 / 工作日”三个词在闪。 | `event.what_day_is_it.start.1` |
| 屏幕角落的像素小人举着牌子：今天到底算哪一种？ | `event.what_day_is_it.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 节假日！ | 乱序 2 张（进事件时随机，不会与其他选项重复） | 指定一件道具获得【再生】 | `holiday` | `event.what_day_is_it.option.holiday` |
| 休息日！ | 乱序 1 张（进事件时随机，不会与其他选项重复） | 生命上限+4 | `rest_day` | `event.what_day_is_it.option.rest_day` |
| 工作日！ | 空（不参与素材匹配，仅作退出/保底选项） | 生命上限-2 → 获得6金币 | `workday` | `event.what_day_is_it.option.workday` |

### 节点 `holiday`

| 正文 | key |
| --- | --- |
| 你把今天定成节假日。系统放起像素烟花，并允许你指定一件道具获得【再生】。 | `event.what_day_is_it.holiday.1` |

### 节点 `rest_day`

| 正文 | key |
| --- | --- |
| 你把今天定成休息日。绷紧的东西松开了一些，生命上限提高4点。 | `event.what_day_is_it.rest_day.1` |

### 节点 `workday`

| 正文 | key |
| --- | --- |
| 你把今天定成工作日。加班扣掉2点生命上限，工资到账6金币。 | `event.what_day_is_it.workday.1` |

---

## 119 · 老硬盘

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_old_hard_drive` |
| 抽牌数 | 3 |
| 标题 | 老硬盘 |
| 标题 key | `event.old_hard_drive.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 抽屉深处翻出一块老硬盘，标签被人用力写过：“备用，勿动”。 | `event.old_hard_drive.start.1` |
| 它转起来的声音，像有人在很远的地方咳嗽。 | `event.old_hard_drive.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 看看有什么 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 失去4点生命 → 获得1张随机方向箭头，带【三重箭头】 | `peek` | `event.old_hard_drive.option.peek` |
| 清空备用 | 乱序 3 张（进事件时随机，不会与其他选项重复） | 删除3张箭头（自选） | `wipe_spare` | `event.old_hard_drive.option.wipe_spare` |
| 换个新的 | 空（不参与素材匹配，仅作退出/保底选项） | 失去5点生命 → 生命上限+4 | `buy_new` | `event.old_hard_drive.option.buy_new` |

### 节点 `peek`

| 正文 | key |
| --- | --- |
| 你强行读取坏道，手指被划出一道血口。硬盘吐出一枚随机方向的【三重箭头】。 | `event.old_hard_drive.peek.1` |

### 节点 `wipe_spare`

| 正文 | key |
| --- | --- |
| 格式化干净得让人愉快。你顺手丢掉3张不再需要的箭头。 | `event.old_hard_drive.wipe_spare.1` |

### 节点 `buy_new`

| 正文 | key |
| --- | --- |
| 拆卸时你流了5点血，但新硬盘的空位让你的生命上限提高了4点。 | `event.old_hard_drive.buy_new.1` |

---

## 120 · 会员到期

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_membership_expired` |
| 抽牌数 | 3 |
| 标题 | 会员到期 |
| 标题 key | `event.membership_expired.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 续费提醒在屏幕上原地弹跳，右上角的红点越积越多。 | `event.membership_expired.start.1` |
| 套餐说明用小字写着：本会员可能不包含你真正想要的东西。 | `event.membership_expired.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 金牌套餐 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 每回合出牌数+1 | `gold_plan` | `event.membership_expired.option.gold_plan` |
| 平价套餐 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 每回合抽牌数+1 | `budget_plan` | `event.membership_expired.option.budget_plan` |
| 停止续费 | 空（不参与素材匹配，仅作退出/保底选项） | 获得3金币 | `cancel` | `event.membership_expired.option.cancel` |

### 节点 `gold_plan`

| 正文 | key |
| --- | --- |
| 金牌套餐生效。每回合你能多打出一张箭头，限制被一行小字悄悄松开了。 | `event.membership_expired.gold_plan.1` |

### 节点 `budget_plan`

| 正文 | key |
| --- | --- |
| 平价套餐只保留了抽牌速度：之后每回合抽牌数+1。 | `event.membership_expired.budget_plan.1` |

### 节点 `cancel`

| 正文 | key |
| --- | --- |
| 你点下停止续费。系统把余额折成3金币退给你，顺手清空了所有红点。 | `event.membership_expired.cancel.1` |

---

## 121 · 不可靠的小帮手

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_unreliable_helper` |
| 抽牌数 | 2 |
| 标题 | 不可靠的小帮手 |
| 标题 key | `event.unreliable_helper.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 一个自称“小帮手”的脚本请求读取你的牌组权限。 | `event.unreliable_helper.start.1` |
| 授权范围有三档，每一档都写着“不会出事”。 | `event.unreliable_helper.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 宽松鉴权 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 获得3张随机方向箭头，带【增殖】 | `loose_auth` | `event.unreliable_helper.option.loose_auth` |
| 严格鉴权 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 获得1张随机方向箭头，带【增殖】 | `strict_auth` | `event.unreliable_helper.option.strict_auth` |
| 完全授权 | 空（不参与素材匹配，仅作退出/保底选项） | 牌组中所有箭头获得【增殖】 | `full_auth` | `event.unreliable_helper.option.full_auth` |

### 节点 `loose_auth`

| 正文 | key |
| --- | --- |
| 小帮手复制出3枚带【增殖】的箭头，然后自顾自地跑掉了。 | `event.unreliable_helper.loose_auth.1` |

### 节点 `strict_auth`

| 正文 | key |
| --- | --- |
| 严格鉴权下，它只复制了1枚带【增殖】的箭头，看起来有点委屈。 | `event.unreliable_helper.strict_auth.1` |

### 节点 `full_auth`

| 正文 | key |
| --- | --- |
| 你把权限全给了它。牌组里每一枚箭头都获得了【增殖】。 | `event.unreliable_helper.full_auth.1` |

---

## 122 · 智能输入法

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_smart_ime` |
| 抽牌数 | 4 |
| 标题 | 智能输入法 |
| 标题 key | `event.smart_ime.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 输入法更新了智能预测，开始抢在你之前打出箭头。 | `event.smart_ime.start.1` |
| 词库管理界面闪着提示：本地空间不足，请谨慎选择。 | `event.smart_ime.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 接入大模型！ | 乱序 3 张（进事件时随机，不会与其他选项重复） | 每回合抽牌数+1 → 将牌库中的基础箭头随机重置（保留附魔） | `llm` | `event.smart_ime.option.llm` |
| 更新词库 | 乱序 2 张（进事件时随机，不会与其他选项重复） | 随机变化2张箭头 | `update_dict` | `event.smart_ime.option.update_dict` |
| 空间不足 | 空（不参与素材匹配，仅作退出/保底选项） | 恢复4点生命 | `no_space` | `event.smart_ime.option.no_space` |

### 节点 `llm`

| 正文 | key |
| --- | --- |
| 大模型接管了联想。每回合多抽1张箭头，代价是所有箭头的方向被它重新猜了一遍。 | `event.smart_ime.llm.1` |

### 节点 `update_dict`

| 正文 | key |
| --- | --- |
| 词库更新完毕，2张箭头被换成它更喜欢的方向。 | `event.smart_ime.update_dict.1` |

### 节点 `no_space`

| 正文 | key |
| --- | --- |
| 空间不足，更新失败。输入法道歉的时候顺手给你回了4点血。 | `event.smart_ime.no_space.1` |

---

## 123 · 新手按键设置

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_input_setup` |
| 抽牌数 | 2 |
| 标题 | 新手按键设置 |
| 标题 key | `event.input_setup.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 首次启动的引导弹窗还没关：请选择你的操作方式。 | `event.input_setup.start.1` |
| 两种方案的代价都写着同一行小字——“会清空当前金币”。 | `event.input_setup.start.2` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 手柄模式 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 花光所有金币 → 获得一次道具奖励 | `gamepad` | `event.input_setup.option.gamepad` |
| 键鼠模式 | 乱序 1 张（进事件时随机，不会与其他选项重复） | 花光所有金币 → 获得一次道具奖励 | `keyboard_mouse` | `event.input_setup.option.keyboard_mouse` |
| 先不设置 | 空（不参与素材匹配，仅作退出/保底选项） | 失去2金币 | `not_now` | `event.input_setup.option.not_now` |

### 节点 `gamepad`

| 正文 | key |
| --- | --- |
| 手柄振动了一下确认映射。金币被清空，换回一包为手柄准备的新手道具。 | `event.input_setup.gamepad.1` |

### 节点 `keyboard_mouse`

| 正文 | key |
| --- | --- |
| 映射写进配置。金币被清空，换回一包为键鼠准备的新手道具。 | `event.input_setup.keyboard_mouse.1` |

### 节点 `not_now`

| 正文 | key |
| --- | --- |
| 你决定稍后再设置。系统扣掉2金币作为“配置暂存费”，态度非常自然。 | `event.input_setup.not_now.1` |

---

## 1001 · 路牌

| 项 | 值 |
| --- | --- |
| 事件 ID | `event_tutorial_signpost` |
| 抽牌数 | 2 |
| 标题 | 路牌 |
| 标题 key | `event.tutorial_signpost.title` |

### 节点 `start`

| 正文 | key |
| --- | --- |
| 你来到了一个三岔路口。面前有一个路牌，上面写着“天才往左，疯子往右”。 | `event.tutorial_signpost.start.1` |

| 选项按钮 | 匹配条件 | 效果 | 结果节点 | key |
| --- | --- | --- | --- | --- |
| 走左边 | 左箭头（按顺序） | 生命上限+10 | `left` | `event.tutorial_signpost.option.left` |
| 走右边 | 右箭头（按顺序） | 失去5点生命 → 删除1张箭头（自选） | `right` | `event.tutorial_signpost.option.right` |
| 为什么要转弯？ | 空（不参与素材匹配，仅作退出/保底选项） | 获得一次道具奖励 | `straight` | `event.tutorial_signpost.option.straight` |

### 节点 `left`

| 正文 | key |
| --- | --- |
| 你选择了左边。生命上限提高了。 | `event.tutorial_signpost.left.1` |

### 节点 `right`

| 正文 | key |
| --- | --- |
| 你选择了右边。疼痛和取舍都很真实。 | `event.tutorial_signpost.right.1` |

### 节点 `straight`

| 正文 | key |
| --- | --- |
| 你拒绝转弯，于是获得了一次道具奖励。 | `event.tutorial_signpost.straight.1` |

---

## 待翻译 key 清单（en-US_Event.json）

116–123 新增事件的英文文本尚未补，需要补齐以下 key：

| key | 中文（参考） |
| --- | --- |
| `event.idle_trade_platform.title` | 闲置交易平台 |
| `event.idle_trade_platform.start.1` | 二手平台的界面泛着旧主机的荧光，卖家头像全是默认的灰色小人。 |
| `event.idle_trade_platform.start.2` | 三个挂着“闲置”标签的箭头排成一列，备注写着：一口价，砍价免谈。 |
| `event.idle_trade_platform.option.cut_to_last` | 砍到最后 |
| `event.idle_trade_platform.option.buyout` | 一口价拍下 |
| `event.idle_trade_platform.option.check_in` | 只是来签到 |
| `event.idle_trade_platform.cut_to_last.1` | 你把价格砍到卖家放弃回复。屏幕一暗，一件商品直接落进你的道具栏。 |
| `event.idle_trade_platform.buyout.1` | 你按下“立即购买”，金币少了3枚。卖家秒发货，一句谢谢都没有。 |
| `event.idle_trade_platform.check_in.1` | 你只点了签到。平台照例给了2金币，然后开始给你推广告。 |
| `event.data_backup.title` | 数据安全 |
| `event.data_backup.start.1` | 维护前的备份提醒弹了出来，磁盘进度条卡在0.3%已经很久。 |
| `event.data_backup.start.2` | 提示写得很客气：全量备份耗时较长，请注意剩余空间。 |
| `event.data_backup.option.full_backup` | 全量备份 |
| `event.data_backup.option.key_backup` | 重点备份 |
| `event.data_backup.option.shortcut_only` | 只复制快捷方式？ |
| `event.data_backup.full_backup.1` | 进度条走到100%。三枚半透明的箭头被完整复制出来——它们不占打出次数，离开手牌就会消失。 |
| `event.data_backup.key_backup.1` | 你只备份了最关键的那一份。一枚【临时】箭头落进牌组，轻得几乎没有重量。 |
| `event.data_backup.shortcut_only.1` | 备份变成了快捷方式：牌组里一半的箭头被随手贴上【临时】标签，指向原件的影子。 |
| `event.what_day_is_it.title` | 今天是什么日子？ |
| `event.what_day_is_it.start.1` | 日历插件崩了，日期位置只剩“节假日 / 休息日 / 工作日”三个词在闪。 |
| `event.what_day_is_it.start.2` | 屏幕角落的像素小人举着牌子：今天到底算哪一种？ |
| `event.what_day_is_it.option.holiday` | 节假日！ |
| `event.what_day_is_it.option.rest_day` | 休息日！ |
| `event.what_day_is_it.option.workday` | 工作日！ |
| `event.what_day_is_it.holiday.1` | 你把今天定成节假日。系统放起像素烟花，并允许你指定一件道具获得【再生】。 |
| `event.what_day_is_it.rest_day.1` | 你把今天定成休息日。绷紧的东西松开了一些，生命上限提高4点。 |
| `event.what_day_is_it.workday.1` | 你把今天定成工作日。加班扣掉2点生命上限，工资到账6金币。 |
| `event.old_hard_drive.title` | 老硬盘 |
| `event.old_hard_drive.start.1` | 抽屉深处翻出一块老硬盘，标签被人用力写过：“备用，勿动”。 |
| `event.old_hard_drive.start.2` | 它转起来的声音，像有人在很远的地方咳嗽。 |
| `event.old_hard_drive.option.peek` | 看看有什么 |
| `event.old_hard_drive.option.wipe_spare` | 清空备用 |
| `event.old_hard_drive.option.buy_new` | 换个新的 |
| `event.old_hard_drive.peek.1` | 你强行读取坏道，手指被划出一道血口。硬盘吐出一枚随机方向的【三重箭头】。 |
| `event.old_hard_drive.wipe_spare.1` | 格式化干净得让人愉快。你顺手丢掉3张不再需要的箭头。 |
| `event.old_hard_drive.buy_new.1` | 拆卸时你流了5点血，但新硬盘的空位让你的生命上限提高了4点。 |
| `event.membership_expired.title` | 会员到期 |
| `event.membership_expired.start.1` | 续费提醒在屏幕上原地弹跳，右上角的红点越积越多。 |
| `event.membership_expired.start.2` | 套餐说明用小字写着：本会员可能不包含你真正想要的东西。 |
| `event.membership_expired.option.gold_plan` | 金牌套餐 |
| `event.membership_expired.option.budget_plan` | 平价套餐 |
| `event.membership_expired.option.cancel` | 停止续费 |
| `event.membership_expired.gold_plan.1` | 金牌套餐生效。每回合你能多打出一张箭头，限制被一行小字悄悄松开了。 |
| `event.membership_expired.budget_plan.1` | 平价套餐只保留了抽牌速度：之后每回合抽牌数+1。 |
| `event.membership_expired.cancel.1` | 你点下停止续费。系统把余额折成3金币退给你，顺手清空了所有红点。 |
| `event.unreliable_helper.title` | 不可靠的小帮手 |
| `event.unreliable_helper.start.1` | 一个自称“小帮手”的脚本请求读取你的牌组权限。 |
| `event.unreliable_helper.start.2` | 授权范围有三档，每一档都写着“不会出事”。 |
| `event.unreliable_helper.option.loose_auth` | 宽松鉴权 |
| `event.unreliable_helper.option.strict_auth` | 严格鉴权 |
| `event.unreliable_helper.option.full_auth` | 完全授权 |
| `event.unreliable_helper.loose_auth.1` | 小帮手复制出3枚带【增殖】的箭头，然后自顾自地跑掉了。 |
| `event.unreliable_helper.strict_auth.1` | 严格鉴权下，它只复制了1枚带【增殖】的箭头，看起来有点委屈。 |
| `event.unreliable_helper.full_auth.1` | 你把权限全给了它。牌组里每一枚箭头都获得了【增殖】。 |
| `event.smart_ime.title` | 智能输入法 |
| `event.smart_ime.start.1` | 输入法更新了智能预测，开始抢在你之前打出箭头。 |
| `event.smart_ime.start.2` | 词库管理界面闪着提示：本地空间不足，请谨慎选择。 |
| `event.smart_ime.option.llm` | 接入大模型！ |
| `event.smart_ime.option.update_dict` | 更新词库 |
| `event.smart_ime.option.no_space` | 空间不足 |
| `event.smart_ime.llm.1` | 大模型接管了联想。每回合多抽1张箭头，代价是所有箭头的方向被它重新猜了一遍。 |
| `event.smart_ime.update_dict.1` | 词库更新完毕，2张箭头被换成它更喜欢的方向。 |
| `event.smart_ime.no_space.1` | 空间不足，更新失败。输入法道歉的时候顺手给你回了4点血。 |
| `event.input_setup.title` | 新手按键设置 |
| `event.input_setup.start.1` | 首次启动的引导弹窗还没关：请选择你的操作方式。 |
| `event.input_setup.start.2` | 两种方案的代价都写着同一行小字——“会清空当前金币”。 |
| `event.input_setup.option.gamepad` | 手柄模式 |
| `event.input_setup.option.keyboard_mouse` | 键鼠模式 |
| `event.input_setup.option.not_now` | 先不设置 |
| `event.input_setup.gamepad.1` | 手柄振动了一下确认映射。金币被清空，换回一包为手柄准备的新手道具。 |
| `event.input_setup.keyboard_mouse.1` | 映射写进配置。金币被清空，换回一包为键鼠准备的新手道具。 |
| `event.input_setup.not_now.1` | 你决定稍后再设置。系统扣掉2金币作为“配置暂存费”，态度非常自然。 |

合计 72 个 key。
